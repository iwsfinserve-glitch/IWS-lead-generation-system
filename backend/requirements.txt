# ── FastAPI & Server ──
fastapi[standard]
uvicorn[standard]

# ── Database (Async) ──
sqlalchemy[asyncio]
asyncpg
psycopg2-binary
alembic

# ── Validation & Settings ──
pydantic[email]
pydantic-settings

# ── Authentication ──
python-jose[cryptography]
bcrypt
python-multipart

# ── Encryption (Google token storage) ──
cryptography

# ── Google Workspace Integration ──
google-api-python-client
google-auth-oauthlib
google-auth-httplib2

# ── AI Reporting ──
google-genai

# ── Report Generation & Charts ──
python-docx
matplotlib
slowapi

# ── Background Scheduler ──
apscheduler

# ── File Parsing ──
openpyxl